import { EditQuoteResponse } from './edit-quote-response';

describe('EditQuoteResponse', () => {
  it('should create an instance', () => {
    expect(new EditQuoteResponse()).toBeTruthy();
  });
});
