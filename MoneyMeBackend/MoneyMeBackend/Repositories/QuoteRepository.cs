using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;
using System;

namespace MoneyMeBackend.Repositories
{
    internal static class QuoteRepository
    {
        //internal async static Task<bool> CreateQuoteAsync(CalculateQuoteRequest quote)
        //{
        //    using (var db = new AppDbContext())
        //    {
        //        try
        //        {
        //            await db.Persons.AddAsync(PersonToCreate);
        //            return await db.SaveChangesAsync() >= 1;
        //        }
        //        catch (Exception)
        //        {
        //            return false;
        //        }
        //    }
        //}

    }
}
